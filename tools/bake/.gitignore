# Never commit real (or generated) telemetry databases — they ship via the app bundle /
# GitHub Releases, exactly like f1db.db. The synthetic seed is regenerated by make_sample_db.py.
*.db
.venv/
__pycache__/
*.pyc
# FastF1's on-disk cache, if pointed here
fastf1cache/
